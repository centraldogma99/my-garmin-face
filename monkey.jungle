# Everything here is the SDK default: sources from source/, resources from
# resources/. Qualified resource directories (resources-eng/) are resolved by
# the resource compiler from the languages declared in manifest.xml — declaring
# them here by hand is what broke the parse.
project.manifest = manifest.xml
